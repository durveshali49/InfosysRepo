/**
 * @deprecated This file is deprecated. Use config/database.js instead.
 * Keeping for backward compatibility during migration.
 */

import pool from './config/database.js';

// Re-export the pool for backward compatibility
export default pool;